package com.test.localizeme.app.currentspeed;

public class MainActivityContent {
    private int userCurrentSpeed;

    public MainActivityContent(int userCurrentSpeed) {
        this.userCurrentSpeed = userCurrentSpeed;
    }

    public int getUserCurrentSpeed() {
        return userCurrentSpeed;
    }
}
